<resources>

    <!-- Base application theme. -->
    <style name="loginTheme" parent="Theme.Design.Light.NoActionBar">
        <item name="android:textColorHint">@color/purple</item>
        <item name="android:colorAccent">@color/colorAccent</item>
    </style>

</resources>
